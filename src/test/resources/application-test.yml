spring:
  datasource:
    username: sa
    password: password
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  domain: http://localhost:8080